# 2026-03-08 — Session marathon (17h-02h39)

## Modifications site seche10semaines.fr (Vercel, seche10-semaines-debug)

### Page /a-propos
- Créée avec bio Hocine B., "Fondateur & Concepteur du programme"
- Références : Mifflin-St Jeor, Björntorp (Obesity Reviews 2001), Nedeltcheva (Annals of Internal Medicine 2010)
- Mention légale : "Ce programme est à visée informative..."
- LinkedIn : https://www.linkedin.com/in/hocine-c-1932653b2/
- Schema Person JSON-LD
- Bug fix : `public/a-propos/index.html` servait en priorité sur la SPA → déplacé dans `_prerender/` pour bots SEO uniquement

### Schema Article (68 articles)
- Ajouté author { Person, "Hocine B.", url: /a-propos } + dateModified: 2026-03-07

### Maillage interne blog
- Articles similaires : Math.random() → système par catégories (articleCategories.ts, 10 catégories)
- 136 liens contextuels inline (2 par article, contextualLinks.ts)
- useMemo sur renderContent + relatedArticles pour réduire INP

### Bug fix critique — articles crashaient
- `renderContent` était une `const` arrow function dans le composant, appelée dans `useMemo` avant sa déclaration → temporal dead zone → "Cannot access 'v' before initialization"
- Fix : déplacée hors du composant (pure function)

## Cloud Functions Firebase

### Séquence drip emails complétée
- DRIP_SCHEDULE : ajouté emails 4 (J+5 témoignage), 6 (J+9 présentation 29€), 7 (J+11 dernier email)
- Séquence complète : J0 guide PDF, J+1 (2,8), J+3, J+5, J+7, J+9, J+11

### Désinscription RGPD
- Page /unsubscribe (React) + Cloud Function `unsubscribeLead` (HTTP)
- Check `unsubscribed === true` dans sendGuideOnLeadCreated, sendDripEmails, runBackfill
- Footer désinscription + header List-Unsubscribe sur tous les emails
- unsubscribeLead met à jour leads ET users

### Backfill system
- `runBackfill()` helper partagé (emails 4, 6, 7, un par lead par exécution)
- `backfillMissingDripEmails` HTTP callable (backup manuel)
- `scheduledBackfill` cron `0 20 * * *` Europe/Paris, 3 exécutions max, état dans `system/backfillStatus`

### trackServerEvent restaurée
- Avait été supprimée par erreur lors d'un `--force` deploy
- Recréée : proxy Facebook CAPI, SHA256 email, IP forwarding, event_id dedup
- Client l'appelle dans `src/utils/analytics.ts:146`

### Bascule Gmail → Brevo SMTP
- `getMailTransporter()` : `service: 'gmail'` → `host: 'smtp-relay.brevo.com', port: 587`
- Credentials : `BREVO_SMTP_LOGIN` + `BREVO_SMTP_PASSWORD` dans `functions/.env`
- From : `huzinepro@gmail.com` → `contact@seche10semaines.fr` (4 occurrences modifiées)
- Domaine vérifié Brevo : DKIM ✅, DMARC ✅, SPF ✅
- Test email envoyé avec succès via Brevo

### Bug fix email 8
- Leads popup/analyseur n'ont pas les données calculateur → email 8 affichait "— kcal"
- Fix : skip email 8 si pas de données, push dans emailsSent pour ne pas bloquer la séquence

### testAllEmails function
- HTTP callable, envoie les 8 emails avec préfixe [TEST X/8]
- Email 8 avec données fictives Marc (42 ans, 85 kg)
- Délai 5s entre envois, 0 écriture Firestore
- Test 8/8 réussi à 02:23

## État des Cloud Functions déployées (11 total)
1. createCheckoutSession
2. stripeWebhook
3. cancelSubscription
4. sendGuideOnLeadCreated (Brevo SMTP, unsub check, footer)
5. sendDripEmails (Brevo SMTP, unsub check, footer, email 8 skip fix)
6. nudgeIncompleteOnboarding
7. unsubscribeLead (NEW)
8. backfillMissingDripEmails (refactoré → runBackfill helper)
9. scheduledBackfill (NEW, cron 20h, 3 runs max)
10. trackServerEvent (restaurée)
11. testAllEmails (NEW)

## Points à surveiller
- scheduledBackfill : 1ère exécution lundi 9 mars 20h → email 4 aux leads éligibles
- INP toujours ~420ms (useMemo aide mais les 1.2MB de data articles restent le bottleneck)
- `functions/.env` contient des secrets en clair (pas de Secret Manager) — pas critique pour l'instant

## Ajouts 02:39-02:47

### X-Mailin-Tag Brevo (en attente deploy)
- Tags ajoutés sur les 5 sendMail pour tracking stats individuelles dans Brevo :
  - `drip-guide`, `drip-email-{N}`, `nudge-onboarding`, `backfill-email-{N}`, `test-email-{N}`
- Build OK, pas encore déployé — Hocine a validé le diff

### Emails mobile responsive
- Templates inline styles + max-width:600px, pas de @media queries (Gmail les ignore)
- Hocine a validé le rendu mobile sur les 8 emails test — "c'est pas choquant, on reste comme ça"
- Léger dépassement sur emails 4 et 5 sur gros chiffres, pas notable

## Session après-midi 08/03 (14h-21h) — Sprint SEO + Conversion + LinkedIn

### Audit SEO blog (68 articles)
- Blog audit CSV généré (`blog-audit.csv`) — 68 articles crawlés
- Audit SEO technique : score 9/9 (sitemap, robots.txt, canonical, OG, SSR, schema.org, alt, lazy loading)
- Trailing slashes harmonisés (canonical + og:url + schema.org alignés sur sitemap avec `/`)
- 6 title tags + meta descriptions réécrits pour CTR (articles page 1 Google avec 0% CTR)

### CTA /pricing ajouté dans les articles blog
- Bloc CTA "PROGRAMME PERSONNALISÉ" injecté après le 2ème H2 dans BlogArticle.tsx (renderContent)
- Design : fond #0F2C59, bouton #00B894, lien vers /pricing

### CTA /pricing ajouté dans Calculateur + Analyseur
- Calculateur : CTA pricing inséré AVANT le email gate, `order: -1` sur mobile
- Analyseur : CTA pricing inséré entre bridge message et recommandations
- Mobile first (43% trafic FacebookApp)

### Trial 7j → 14j
- `trial_period_days: 14` dans createCheckoutSession
- Toutes les mentions "7 jours" → "14 jours" sur Landing, Pricing, Analyseur, BlogArticle, StripeCheckout, emails drip
- Rappel cron programmé le 30 avril pour renouveler le token LinkedIn

### Page /pricing — messaging simplifié
- Supprimé : "ALGORITHME SCIENTIFIQUE", "Dernière étape", jargon Mifflin-St Jeor dans les features
- Ajouté : "Perdez 8-12 kg en 10 semaines", "On vous dit quoi manger. Vous exécutez.", "Basé sur des études validées" en petit

### Emails drip — CTA vers /pricing
- Emails 2-7 : tous les CTA pointent vers /pricing (plus aucun vers /setup)
- PS texte brut avec lien /pricing ajouté sur chaque email
- Nouvel email 9 ajouté (J+2) : "Ton plan personnalisé est prêt — 0€ pour commencer"
- DRIP_SCHEDULE réordonné : J1→8, J2→9, J3→2, J5→3, J7→4, J9→5, J11→6, J13→7
- Tutoiement harmonisé sur tous les emails (guide, 8, 9, nudge)

### LinkedIn publication automatique
- 68 posts LinkedIn générés (`scripts/linkedin/linkedin-posts.json`) — ton marque, hooks choc, image_suggestion
- Script `linkedin-publisher.cjs` : publie via API REST v2, lien en fin de post
- Post #1 publié (URN: `urn:li:share:7436422464622174208`)
- Cron quotidien 8h Paris configuré
- Token OAuth expire le 7 mai 2026, rappel cron le 30 avril
- `linkedin-config.json` dans .gitignore (contient access_token + person_urn)
- Commentaire auto bloqué (403 — besoin Community Management API = app séparée)
- Config : client_id=7814yj5vh78ftk, person_urn=urn:li:person:gygD2msLxI

### Sécurité vérifiée
- `functions/.env` dans .gitignore ✅, jamais committé ✅
- `sk_live_` absent de l'historique git ✅
- `linkedin-config.json` dans .gitignore ✅
- Sender email = `contact@seche10semaines.fr` partout (aucune trace de huzinepro)

### Vérification post-deploy : 21/22 ✅
- Seul point ⚠️ : testAllEmails timeout (cold start) → fixé : délai réduit 5s→2s, email 9 ajouté dans le test, 9/9 envoyés OK

### Réécriture complète emails v3 (en cours)
- 9 emails réécrits dans `email-rewrite-v3.md` — en attente validation Hocine
- Changements : HTML léger (pas de bandeau bleu), tutoiement partout, 1 CTA/email, teasers, sources citées
- Nouvel emailNum 10 (objections FAQ) à J12, suppression emailNum 9 redondant
- DRIP_SCHEDULE : J1(8), J2(2), J4(3), J6(4), J8(5), J10(6), J12(10), J14(7)
