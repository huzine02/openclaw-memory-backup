# 2026-03-13 — Session massive Sèche 10 Semaines

## Emails Trial Engagement (7 emails)
- Créé `sendTrialEngagementEmails` Cloud Function (scheduler toutes les heures, 7h-20h Paris)
- 7 emails : J0 welcome, J1 première journée, J3 motivation, J5 rappel fin, J7 dernier jour, J10 relance, J14 dernière chance
- Signé "Jim" (pas Hocine, pas "L'équipe")
- Champ `trialEmailsSent: string[]` dans users/ pour tracker les envois
- `testTrialEmail` Cloud Function HTTP pour tester n'importe quel email
- 7 tests envoyés sur huzine@gmail.com — validés

## Landing Page Refonte Freemium
- Tous les textes mis à jour : "essai gratuit" → "accès gratuit 7 jours", tous CTA → /quiz
- Supprimé lien analyseur du hero, code LANCEMENT50, places restantes
- FAQ mise à jour pour le modèle freemium
- Header nav : "Calculateur" → "Quiz"
- Footer : "Quiz gratuit" → /quiz

## Quiz Polish Visuel
- Slide transitions entre écrans (slideInLeft/slideInRight 300ms)
- Count-up animé sur résultats (TDEE, macros)
- Interstitielle motivante : 5s (au lieu de 3), count-up 73%, radial gradient
- Interstitielle calcul : colonnes data matrix en fond
- Cards : hover scale + checkPop sur checkmark
- Number bounce sur steppers âge/poids/taille
- Google OAuth button pulse glow
- Cadenas pulse sur projection floutée

## Écran 0 Quiz — Phone Mockup
- Split screen : texte à gauche, phone frame rotatif à droite (3 écrans Dashboard/Journal/Mon Plan)
- Phone HTML identique à Landing.tsx (pas d'images)
- Mobile responsive : phone 160x320, texte centré, padding-top 60px
- Badges flottants supprimés, phone mis droit (pas de rotation)
- Textes personnalisés : "Ton programme de sèche personnalisé t'attend"

## Reverse Trial 7 jours — Fix Critique
- `isPremium()` dans subscription.ts : ajouté support `free_trial` + `trialEndDate`
- `free_trial` ajouté au type `SubscriptionStatus`
- Dashboard/Journal/Guide/Profile passent `trialEndDate` à `isPremium()`
- Pendant 7j : accès complet. Après : accès restreint (semaine 1, journal 5j)

## Audit Complet (64 points)
- 3 ❌ critiques trouvés et corrigés :
  1. UTM params perdus /calculateur→/quiz : fixé via redirect 301 dans vercel.json (server-side, préserve params)
  2. Bandeau "X jours restants" absent : créé TrialBanner.tsx (bleu/orange/rouge selon jours), ajouté dans Layout.tsx
  3. fbq CompleteRegistration manquant : ajouté appel direct fbq à l'écran 10

## Stripe — Coupon LANCEMENT50
- Coupon créé via API : 50% off, duration forever, id "LANCEMENT50"
- Prix reste 29€/mois, coupon appliqué automatiquement dans createCheckoutSession
- `discounts: [{ coupon: 'LANCEMENT50' }]` sur toutes les sessions
- User free_trial → trial_period_days: 0 (paiement immédiat 14,50€)
- User direct → trial_period_days: 14 puis 14,50€/mois

## Cloud Functions — Anciens Drip ENFIN Déployés
- sendDripEmails, nudgeIncompleteOnboarding, backfillMissingDripEmails, scheduledBackfill : tous ont return en début
- **DÉPLOYÉ** le 13/03 à ~14h55 — `firebase deploy --only functions` (toutes les 18 functions)
- 34 anciens emails étaient partis ce jour car le return n'avait JAMAIS été déployé avant

## Bug Critique — "Chargement du profil..." infini
- Root cause : `setAuthUid(uid)` puis `goNext()` — React state async, authUid encore null quand useEffect step 10 se lance
- `if (authUid)` skip le setDoc → doc users/{uid} jamais créé → dashboard bloqué
- Fix : `if (!authUid) return;` au début du useEffect step 10 — re-run quand authUid arrive via deps
- Aussi : redirect ne se fait qu'après save Firestore terminé (plus de timer fixe 6s)

## Paths critiques
- Projet : `C:\Users\huzine\projects\seche10-semaines`
- Vercel project : `seche10-semaines-debug`
- Firebase project : `seche10semaines`
- Price ID Stripe : `price_1T2ptbDWHAsAimG7RUeXVrjR` (29€, avec coupon LANCEMENT50 = 14,50€)
- Stripe secret : dans functions/.env

## Fix Auth Email — signInAnonymously DÉSACTIVÉ (16h36)
- `signInAnonymously` échoue : ADMIN_ONLY_OPERATION = auth anonyme désactivée dans Firebase Console
- Testé via API identitytoolkit : confirmé
- **Fix** : remplacé par `createUserWithEmailAndPassword` avec mot de passe auto-généré
- Fallback chain : createUser → signInAnonymously → lead-only (sans auth Firebase)
- Si `lead-only-*` : sauvegarde lead/ seulement, pas de doc users/, redirect vers / au lieu de /dashboard
- Si email déjà utilisé (auth/email-already-in-use) → fallback anon ou lead-only

## Fix "Chargement du profil..." — AuthContext safety net (16h36)
- Root cause : user auth existe dans Firebase Auth mais pas de doc users/{uid}
- Cas : anciens comptes, comptes Google existants, quiz échoué avant save
- **Fix** : AuthContext.fetchProfile crée un profil minimal si doc n'existe pas
- Profil minimal : onboardingComplete: false → redirige vers /setup → /quiz
- Plus jamais de blocage infini sur "Chargement du profil..."

## Firebase Auth — Providers à vérifier
- Google : activé ✅
- Email/Password : À VÉRIFIER (nécessaire pour le nouveau flow createUserWithEmailAndPassword)
- Anonymous : DÉSACTIVÉ — cause des erreurs si fallback utilisé
- Hocine doit activer Email/Password dans Firebase Console → Authentication → Sign-in method
