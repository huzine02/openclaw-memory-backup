# 2026-03-14 — Sèche 10 Semaines — Journée majeure

## Quiz V3 déployé (6 écrans, positionnement santé métabolique)
- Ancien quiz: 11 écrans, 10% conversion (49→5)
- Nouveau quiz: 6 écrans, ~100% conversion quiz→inscription (15 leads = 15 users)
- Flow: Hero → Déclencheur (ventre/sang/médecin/santé) → Biométrie (1 écran) → Activité → Santé simplifié → Email gate → Résultats+redirect
- Prénom supprimé, 2 interstitielles supprimées, email AVANT résultats
- In-app browser: Google OAuth masqué, email seul avec autoFocus
- `data.declencheur` nouveau champ stocké dans Firestore
- `activite` keys changés: sedentaire/leger/actif/tres_actif (au lieu de modere/actif)
- `profilSante` nouveau: glycemie_cardio pour "les deux"
- Fichier backup: Quiz.tsx.backup

## Auth email refactorisé
- Mot de passe déterministe: `S10S_${btoa(email).slice(0, 16)}`
- Même email = même UID = mêmes données au retour
- Fallback: signInAnonymously si le pwd ne match pas (anciens users)
- signInWithRedirect pour Google en in-app browser + getRedirectResult au mount

## Cloud Functions modifiées
- sendGuideOnLeadCreated: DÉSACTIVÉ (return en début)
- sendTrialEngagementEmails: 3 emails comportementaux ajoutés
  - `inactive-24h`: user inactif >24h, aucune pesée ni journal
  - `first-journal`: exactement 1 entrée journal (renforcement positif)
  - `pricing-visit`: visitedPricing=true en Firestore
  - Throttle: lastTrialEmailAt, pas d'email si <4h depuis le dernier
- Journal subcollection: users/{uid}/journal
- Pesées: users/{uid}.pesees (array dans doc principal)

## Dashboard — Progressive disclosure + Checklist onboarding
- Checklist "🚀 Tes premiers pas": 3 tâches (plan/poids/journal), disparaît quand tout fait
- `seenPlan` en localStorage (s10s_seen_plan)
- 4 niveaux: L0=rien, L1=poids, L2=journal, L3=tout
- Score Santé: flouté+verrouillé L0-L1, visible L2+ avec message "partiel" si pas de glycémie
- Streak/Badges: floutés visibles L0-L2, débloqués L3
- Sections `id="section-programme"` et `id="section-poids"` pour scroll ancrage
- `visitedPricing: true` ajouté dans Pricing.tsx

## Fixes mineurs
- LockedField: tout le champ = Link vers /pricing avec cursor:pointer
- Semaines verrouillées: clic → /pricing au lieu de return silencieux
- Prénom: .trim() sur Google OAuth et écriture Firestore

## Métriques
- 15 leads + 15 users aujourd'hui (14 vrais + 1 test)
- Tous via quiz, tous free_trial
- 0 checkout Stripe
- Rapport matin: 35 users total, 13 free_trial, 1 payant, 4 actifs (pesée)

## À surveiller
- Taux de returning users demain (15 mars) — premier vrai test du nouveau quiz + emails
- Premier trial-j1 envoyé vers 17h aujourd'hui pour les users du 13/03
- Emails comportementaux (inactive-24h) pour les users du 14/03 → demain ~16h
