# 2026-03-21

## 3 Prompts déployés (nuit du 20→21 mars)

### Prompt 1 — Âge Métabolique (Quiz)
- Fonction `calculateMetabolicAge` avec table McMurray, interpolation linéaire
- Jauge SVG circulaire animée, couleur dynamique
- Sauvé dans Firestore users/ et leads/, event analytics
- Fix: metaAfter était Math.random() → remplacé par formule déterministe

### Prompt 2 — Coach IA Nutrition
- Cloud Function `chatWithCoach` (Gemini Flash, gratuit)
- Auth Firebase, rate limit 50/20/5 messages/jour, garde-fou médical
- Composant `CoachChat.tsx` — bulle flottante, panel slide-up
- Visible sur Journal + Dashboard
- Clé Google AI créée sur projet GCP seche10semaines

### Prompt 3 — Décrypteur Bilan Sanguin
- Cloud Function `analyzeBloodTest` (Gemini Flash)
- Premium only, 21 marqueurs, normes de référence, flag auto
- Score santé métabolique /100
- Rate limit 3 analyses/jour
- Composant `BloodAnalyzer.tsx` — visible Dashboard si profil glycemie/cardio
- Historique dans bloodAnalyses subcollection

### Audit réalisé
- 0 bug bloquant après fix metaAfter
- TypeScript compile sans erreur
- Toutes les URLs Cloud Functions corrigées
- Disclaimers médicaux en place

---

## Session matin 21/03 (09h-10h20 Paris)

### Métriques confirmées
- **0 payant réel** — les 9 "active" en base sont tous des comptes Hocine/tests sans Stripe
- 2 leads frais ce matin : coxi35@live.fr (06:47) et perrotgaetan@outlook.fr (07:19)
- MEMORY.md mis à jour (payants = 0)

### Bug fix — Âge Métabolique (Quiz)
- **Problème** : formule utilisait le BMR brut → un mec de 95 kg obtenait 36 ans métaboliques (absurde)
- **Fix** : normalisation au poids idéal (Devine) + pénalité surpoids (+2 ans / 10% excès)
- Ex: homme 46 ans, 95 kg, 175 cm → 53 ans métaboliques (avant: 36)
- Déployé Vercel prod

### Fix — Coach IA Nutrition (`chatWithCoach`)
- Prompt refait : personnage "Jim", coach masculin, ton direct, réponses complètes avec grammages
- `maxOutputTokens` : 400 → 2048
- Ajout historique conversationnel (10 derniers messages envoyés à Gemini)
- Corrigé double backtick dans template literal (`;` en trop → erreur compile)
- Déployé Firebase

### Fix — Stepper Poids Quiz
- Step changé de 0.5 à 1 kg
- Puis remplacé par **input numérique direct** (type=number, inputMode=numeric) pour âge/poids/taille
- L'user peut taper directement au lieu de cliquer 17 fois
- Boutons +/- toujours présents pour ajustement fin

### Coach Chat — Visibilité
- Bouton flottant : bulle anonyme 💬 → bouton avec label **"💬 Coach Jim"**
- Teaser auto après 3s : "Jim, ton coach 🏋️ — Une question nutrition ? Demande-moi !"
- Teaser stocké dans localStorage (`s10s_coach_teaser_seen`) → apparaît 1 seule fois

### Refonte Décrypteur Bilan Sanguin (`/decrypteur`)
- **Ancien tunnel** : 9€ en pleine face → personne paie
- **Nouveau tunnel** :
  1. Hero : Upload GRATUIT + preview exemple (1 marqueur visible, 2 floutés, score 62/100)
  2. Preview : "Ton bilan est prêt" + liste features + prix 9€ + CTA
  3. Upload post-paiement (fichier récupéré de localStorage après redirect Stripe)
  4. Résultat complet avec CTA vers /quiz
- URL checkout corrigée : `createbloodcheckout-vyniq7423a-uc.a.run.app` (était `us-central1-seche10semaines.cloudfunctions.net`)

### Bypass test temporaire
- `stripeSessionId === 'TEST_FREE_BYPASS_2026'` dans `analyzeBloodTest`
- Auth rendue optionnelle pour ce bypass
- **⚠️ À RETIRER après test** — actif en prod

### Bug fix — pdf-parse
- `pdfParse(buffer)` → `new PDFParse({ data: buffer })` puis `.getText()`
- L'API de pdf-parse a changé : c'est une classe `PDFParse`, pas une fonction
- Déployé Firebase

### URLs Cloud Functions confirmées (hash `vyniq7423a`)
- chatWithCoach: `https://chatwithcoach-vyniq7423a-uc.a.run.app`
- analyzeBloodTest: `https://analyzebloodtest-vyniq7423a-uc.a.run.app`
- createBloodCheckout: `https://createbloodcheckout-vyniq7423a-uc.a.run.app`
- Toutes les URLs frontend dans src/ pointent vers le bon hash
