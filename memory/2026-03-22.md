# 2026-03-22 — Session marathon Sèche 10 Semaines

## Gemini API — IMPORTANT
- `gemini-2.0-flash` retiré par Google → 404. Migré vers `gemini-2.5-flash`
- `thinkingConfig: { thinkingBudget: 0 }` n'est PAS supporté par l'API REST Gemini → erreur 400 "Unknown name"
- `responseMimeType: 'application/json'` fonctionne avec gemini-2.5-flash
- maxOutputTokens augmenté à 16384 pour éviter troncature JSON
- Nettoyage fallback ajouté : `raw.replace(/€/g, '').replace(/,\s*([}\]])/g, '$1')`

## Shopping List (Prompt 8)
- Cloud Functions `generateWeeklyShoppingList` (scheduled dimanche 10h) + `triggerShoppingList` (HTTP on-demand)
- Frontend: `src/components/ShoppingList.tsx` avec accordéon repliable (useState AVANT les early returns !)
- Stockage: `users/{uid}/shoppingLists/{date}` dans Firestore
- Email hebdo via Brevo avec lien `/profile` (corrigé de `/profil`)

## React Hook Rules — Bug critique
- JAMAIS mettre un `useState` après un `if (!data) return` → React error #310 "Rendered more hooks than previous render"
- Les hooks placeholder `const [_unused] = useState()` sont tree-shaked par esbuild → ne marchent pas comme workaround
- Toujours garder les noms originaux des useState même si inutilisés

## Encodage fichiers articles
- `seoArticles25.ts`, `seoArticlesRewrite.ts`, `healthArticles.ts` étaient en Windows-1252
- Fix: lire en latin-1, réécrire en UTF-8 via `[System.Text.Encoding]::GetEncoding(1252)`

## Refonte visuelle (Prompt Design)
- Palette: emerald (#059669) + stone (#fafaf9) remplace l'ancien bleu marine
- Typo: Plus Jakarta Sans (titres) + DM Sans (body) ajoutées dans index.html
- theme.ts: lightTheme + darkTheme mis à jour
- BottomNav: fond blanc, icônes SVG, pilule verte active, labels 11px
- WelcomeCard: gradient emerald, 3 mini-cards (déficit/objectif/streak), visible au-delà de J7
- Journal: donut SVG calories + barres macros colorées (violet/amber/rouge)
- MealCardV2: cards blanches, boutons "J'ai suivi" / "Changer"
- CoachChat: pilule emerald fixed bottom 80px right 16px, tooltip au-dessus auto-dismiss 5s

## Profil — Réorganisation
Nouvel ordre: Info user → Cibles du jour (card sombre) → Bilan progression → Liste courses → Jeûne IF → Santé métabolique → Score santé → Streak/Badges → Photos → Ressources → GLP-1 → Parrainage → Abonnement → Apparence → Paramètres

## Profil — Changements
- Jeûne IF: 3 fenêtres (12-20, 13-21, 14-22) + bouton "Désactiver", débloqué pour isPremium (pas seulement isPaid)
- Parrainage déplacé en bas (avant Abonnement)
- Modal liste courses statique SUPPRIMÉE (remplacée par composant IA ShoppingList)
- Doublon bloc Métabolisme supprimé (remplacé par card "Cibles du jour")

## GLP-1 Rappels
- Compte hocine.bouhend@hotmail.com avait glp1Config: null → config set (ozempic, lundi 10h, reminderEnabled: true)
- Compte huzinepro avait reminderEnabled: false → activé

## Journal — Mode par défaut
- Remis `mealMode: 'plan'` par défaut (pas 'scan') — les repas doivent être visibles en premier
- Boutons "Mes repas" / "Scanner IA" équilibrés (50/50), plus de "ou" entre eux

## 5 nouveaux leads ce matin (22/03)
- sponcelet18@gmail.com, mervill.emmanuel@orange.fr, i.abram@hotmail.fr, vitou.laplace@gmail.com, chantal-gravier@wanadoo.fr
- Tous free_trial, arrivés entre 5h et 7h via Meta Ads

## Cron jobs — État
- linkedin-daily-post: 6 erreurs consécutives (channel manquant multi-channel)
- Rapport quotidien: timeout 120s (4 erreurs)
- LinkedIn token expire 7 mai 2026

## Bugs restants connus
- Scanner IA: bouton "Prendre une photo" coupé par la bottom nav
- Petit-déjeuner: pas de macros P/G/L affichées (contrairement aux 3 autres repas)
- Header "AUJOURD'HUI / date" apparaît 2x dans le journal (sticky header dupliqué visuellement)
