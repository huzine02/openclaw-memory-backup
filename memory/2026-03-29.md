# 2026-03-29

## Leads du jour
- 3 nouveaux leads (hors test Hocine) : guillinstephanie4@gmail.com, d.une@hotmail.fr, carmencita.bgn@gmail.com
- 1 checkout_start à 12h58 (email non capturé dans funnel)

## PWA Android — manifest fix
- `start_url` changé de `/` à `/dashboard` dans `public/manifest.json`
- Landing page : ajout redirect auto vers `/dashboard` si user connecté (useNavigate + onAuthStateChanged)
- Pour prendre effet : supprimer icône, re-ajouter depuis Chrome

## Encodage corrompu — fichiers réécrits
- `SubscriptionBanner.tsx` : accents cassés (accès, débité, etc.) → réécrit avec unicode escapes
- `TrialUpgradeModal.tsx` : même problème → réécrit complet
- `LeadMagnetPopup.tsx` : OK, pas touché
- Rappel : JAMAIS utiliser PowerShell Set-Content sur ces fichiers

## Profil — email visible
- Ajout `user?.email` sous le nom dans `Profile.tsx` (ligne ~440)

## MealScanner — caméra in-app (getUserMedia)
- Bug : Android PWA tue l'app quand caméra externe s'ouvre → perte de contexte au retour
- Fix : bouton "Prendre une photo" utilise maintenant `getUserMedia` avec UI caméra fullscreen in-app
- Fallback : si getUserMedia échoue → file input galerie
- Galerie fonctionne toujours via input ref classique

## Glp1 / Ozempic — nettoyage profil test
- Compte huzinepro@gmail.com avait `glp1Treatment: "ozempic"` et `healthProfile: "prediabetes"` (résidu de test)
- Le coach IA et le module Glp1 lisaient ces champs → conseils Ozempic inappropriés
- Supprimé `glp1Treatment` et `glp1Config` du doc user `DvYfAe9HaJR4d7OEYbAEu2nfKhI3`
- Le module Glp1 ne s'affiche que si `userProfile.glp1Treatment` est défini (pas activé par défaut)
